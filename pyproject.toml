[project]
name = "neutschbot"
version = "0.1.0"
description = "Posts strengthened verbs and other innovations to Mastodon"
requires-python = ">=3.11"
dependencies = ['beautifulsoup4', 'Mastodon.py', 'requests']
